# Core
anthropic>=0.40.0
openai>=1.0.0          # free providers: NVIDIA / Groq / Gemini / Ollama (OpenAI-compatible)
ddgs>=6.0             # free web search for the Trend Scout
pyyaml>=6.0
python-dotenv>=1.0.0
rich>=13.7.0

# Download + transcription
yt-dlp>=2024.0.0
faster-whisper>=1.0.0
opencv-python-headless>=4.8    # face-tracking reframe (Haar cascade, no GPU needed)

# Uploading (YouTube)
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
edge-tts
